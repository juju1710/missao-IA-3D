const nomes = [ "Igor", "Aghata", "Albert", "Ariana", "Alex", "Dylan"];

export funcition aleatorio (lista){
  const posicao = math.floor(math.randon()* lista.lenght);
  return lista(posicao);
}

export const nome = aleatorio(nome)
